const Pagination = () => {
  return (
    <div>
      {/* <Stack spacing={2}>
      <Pagination
        count={16}
        defaultPage={1}
        shape="rounded"
        siblingCount={-1}
        boundaryCount={2}
        color="standard"
      /> */}
    </div>
  );
};

export default Pagination;
